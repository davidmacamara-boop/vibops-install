apiVersion: v2
name: vibops
description: VibOps — Intent-driven MLOps platform
type: application
version: 0.41.5
appVersion: "0.41.5"

dependencies:
  - name: postgresql
    version: "15.5.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
